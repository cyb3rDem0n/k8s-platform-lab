apiVersion: v1
kind: Namespace
metadata:
  name: ai
  labels:
    # Ollama gira come root nell'immagine ufficiale: usiamo "baseline" + securityContext stretto.
    # Esercizio avanzato nel tutorial: portarlo a "restricted" con runAsUser e HOME scrivibile.
    pod-security.kubernetes.io/enforce: baseline
    pod-security.kubernetes.io/warn: restricted
