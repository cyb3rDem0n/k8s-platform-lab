apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: ai-ollama
  namespace: argocd
  annotations:
    argocd.argoproj.io/sync-wave: "0"
  finalizers:
    - resources-finalizer.argocd.argoproj.io
spec:
  project: lab
  source:
    repoURL: https://github.com/cyb3rdem0n/k8s-platform-lab.git
    targetRevision: main
    path: k8s/apps/ai/ollama
  destination:
    server: https://kubernetes.default.svc
    namespace: ai
  syncPolicy:
    automated: { prune: true, selfHeal: true }
    syncOptions:
      - CreateNamespace=false   # il Namespace è nel manifest, con le sue etichette PSA
